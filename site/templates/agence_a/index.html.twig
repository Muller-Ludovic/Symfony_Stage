{% extends 'base.html.twig' %}

{% block title %}Agence A{% endblock %}

{% block body %}
    <h1>Agence A</h1>
        <div class="container">
            <div class="row">
                {% for agenceA in agenceA %}
                    <div class="col-12 col-md-6">
                            <div class="card mb-4">
                                <div class="card-header">
                                    <img src="https://picsum.photos/300/200" class="Hero-new"/>
                                    <p><span class="bold"> Nom : </span>{{ agenceA.nom }}</p>
                                    <p><span class="bold">Prénom : </span>{{ agenceA.prenom }}</p>
                                    <p><span class="bold">Entreprise : </span>{{ agenceA.entreprise }}</p>
                                    <p><span class="bold">Email : </span>{{ agenceA.email }}</p>
                                    <p><span class="bold">Tel : </span>{{ agenceA.tel }}</p>
                                </div>
                            </div>
                    </div>
                {% endfor %}
            </div>
        </div>
{% endblock %}