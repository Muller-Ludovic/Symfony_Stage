{% extends 'base.html.twig' %}

{% block title %}Agence C{% endblock %}

{% block body %}
    <h1>Agence C</h1>
        <div class="container">
            <div class="row">
                {% for agenceC in agenceC %}
                    <div class="col-12 col-md-6">
                            <div class="card mb-4">
                                <div class="card-header">
                                    <img src="https://picsum.photos/200/100" class="Hero-new"/>
                                    <p><span class="bold"> Nom : </span>{{ agenceC.nom }}</p>
                                    <p><span class="bold"> Prénom : </span>{{ agenceC.prenom }}</p>
                                    <p><span class="bold"> Entreprise : </span>{{ agenceC.entreprise }}</p>
                                    <p><span class="bold"> Email : </span>{{ agenceC.email }}</p>
                                    <p><span class="bold"> Tel : </span>{{ agenceC.tel }}</p>
                                </div>
                            </div>
                    </div>
                {% endfor %}
            </div>
        </div>
{% endblock %}