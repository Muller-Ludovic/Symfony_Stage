{% extends 'base.html.twig' %}

{% block title %}Articles{% endblock %}

{% block body %}
    <h1>Articles</h1>
<div class="container">
    <div class="row">
        {# ici on veut afficher tous les articles #}
        {% for articles in articles %}
        <div class="col-12 col-md-6">
            {{ include('articles/_article.html.twig') }}
        </div>
        {% endfor %}
    </div>
</div>
{% endblock %}